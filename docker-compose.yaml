version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: seapedia-postgres
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - seapedia-db-data:/var/lib/postgresql/data

volumes:
  seapedia-db-data: